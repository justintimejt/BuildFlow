fastapi==0.115.0
uvicorn[standard]==0.30.5
supabase==2.24.0
python-dotenv==1.0.1
google-generativeai==0.8.3
importlib-metadata>=6.0.0

# Note: Frontend dependencies are managed in frontend/package.json
# Recent frontend dependencies added (see frontend/package.json):
# - class-variance-authority (for shadcn UI components)
# - clsx (for conditional class names)
# - tailwind-merge (for merging Tailwind classes)
# - lucide-react (for icons in shadcn components)
# - @radix-ui/react-slot (for Button component)
# - @radix-ui/react-dropdown-menu (for DropdownMenu component)
# - @radix-ui/react-avatar (for Avatar component in chat interface)
# - @types/node (TypeScript types for Node.js)

